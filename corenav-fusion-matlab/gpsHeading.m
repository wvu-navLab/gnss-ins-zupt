% starts(:,:)=find(lin_x>0.0);
% latitude_now=llhGPS(1,700);
% latitude_prev=llhGPS(1,6);
% longitude_now=llhGPS(2,700);
% longitude_prev=llhGPS(2,6);
%
% Xhead=cos(latitude_now*pi/180)*sin(longitude_now*pi/180-longitude_prev*pi/180);
% Yhead=cos(latitude_prev*pi/180)*sin(latitude_now*pi/180)-sin(latitude_prev*pi/180)*cos(latitude_now*pi/180)*cos(longitude_now*pi/180-longitude_prev*pi/180);
% yaw=atan2(Xhead,Yhead);
% longrun start2800 ENU